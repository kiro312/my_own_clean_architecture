name: my_own_clean_architecture
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  build_runner: ^2.4.13
  connectivity_plus: ^6.1.0
  dartz: ^0.10.1
  data_connection_checker_tv: ^0.3.5-nullsafety
  dio: ^5.7.0
  flutter:
    sdk: flutter
  get_it: ^8.0.0
  json_annotation: ^4.9.0
  json_serializable: ^6.8.0
  logger: ^2.4.0
  pretty_dio_logger: ^1.4.0
  provider: ^6.1.2
  retrofit: ^4.4.1
  retrofit_generator: ^9.1.2
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
